import pygame
# pygame 加载并且初始化pygame的所有模块
pygame.init()
# 游戏中的坐标系
# 原点在主窗口的左上角(0, 0) 水平为x轴，向右递增 垂直为y轴，向下递减
# 在游戏中，所有可见的元素都是以矩形区域描述
# 描述一个矩形区域有四个要素：(x,y)(width,height)
# 编写游戏的代码
print("游戏代码")

# 在游戏退出之前卸载游戏的所有模块
pygame.quit()
